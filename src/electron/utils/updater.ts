/**
 * Automatic official FreeShow updates are disabled in GocShow.
 * Upstream releases must be merged, reviewed, tested and rebuilt manually.
 */
export default async function checkForUpdates(): Promise<void> {
    return
}
